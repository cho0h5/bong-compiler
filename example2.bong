int foo(int x) {
	int y;
	y = x + 123;
}

void bar() {
	foo(20);
}

int main(int a, int b) {
  int c;
  int d;
	c = ( -20 - a ) * b;
  int i;
	if (c) {
		bar();
	};
	while (i >= 0) {
	};
	return d[0];
}
