int fib(int a) {
  if (a < 1) {
    return 0;
  };

  if (a < 2) {
    return 1;
  };
  int fib1;
  fib1 = fib(a - 1);
  int fib2;
  fib2 = fib(a - 2);
  return fib1 + fib2;
}

void bong() {
  print_int(fib(10));
}
