// builtin function
// 1. print_integer(int n);
// 2. print_char(int n);
// 3. exit();

int fib(int n) {
	if (n <= 1) {
		return 0;
	}
	return fib(n - 1) + fib(n - 2);
}

void main() {
	int a = fib(10);
	print_integer(a);
}
